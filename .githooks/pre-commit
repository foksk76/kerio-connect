#!/usr/bin/env bash
set -euo pipefail

repo_root="$(git rev-parse --show-toplevel)"
"${repo_root}/scripts/update-commit-docs.sh"
git add "${repo_root}/HANDOFF.md" "${repo_root}/NEXT_STEPS.md" "${repo_root}/CHANGELOG.md"
